# Bounded post-repair monitoring

Run: `inquiry-run-23e0700d8dc045979be55004fb5ea135`. Read-only checkpoints around 234–272 paid calls, after the 172,000-arrival controlled resume. Counts describe this interval, not final totals.

At the later checkpoint, the run had admitted 196,000 records, made 272 paid calls, and recorded approximately $2.18645 in model-returned cumulative usage. The interval's 38 posted actions were one open, one reply, eight reads, five searches, thirteen waits, and ten watches. There were no new schema failures or false/unknown reply-target rejections. Four action rejections were two duplicate reads and two watch changes lacking evidence or an explicit unresolved premise. The latter attached CPUC/subpoena and GE/Consent/Assignment watches to the existing database-privileges inquiry; these do not establish a coherent new investigation.

The one new inquiry concerned execution of an ABB authorization letter for the Austin Project. It produced a real host-assisted addressed question and a correctly linked peer reply saying no relevant evidence was available. This demonstrates request/reply plumbing, not successful collective discovery. The host had recruited on generic overlap including “document,” “letter,” and “project,” without an ABB, Austin, or named-person match. This was a concrete relevance defect in the automatic probe gate.

The subsequent file-only repair requires a named matter/person token from the original question to match observed peer coverage, with generic question and paperwork terms excluded. No usable anchor means no automatic probe; agent-initiated requests remain unrestricted. Tests reject the generic paperwork match, accept an older actually matching ABB/Austin descriptor, and preserve once-only probing, budgets, and nonmembership. The matching protocol records its version and activation watermark. This repair requires a controlled resume; it does not change a running process or its checkpoint.

Earlier fixes removed synthetic artifact messages, so artifacts no longer create counterfeit conversational reply targets. Assigned artifact evidence remains scoped memory, and unassigned inquiries remain a slim directory during exploration. Offline regressions check these boundaries and preservation of real addressed requests. This monitoring did not audit every private prompt or establish semantic entailment, causal discovery, or that all admitted mail was read by a model.
